function Coffee() {
  return (
    <div>
      <h1>I am a coffee shop</h1>
      <p>Welcome to our coffee shop! from React</p>
    </div>
  );
}
export default Coffee;
// in App.js we can only return one element but can use the fragment <> </> for multiple elements and for react we use  npm react start and for the vite we use npm vite dev

//  Also we don't need to use .jsx but we can use as well not a problem in react but in vite we need to use .jsx as file extension,
// In both need to capitalize the first letter  function name
//
